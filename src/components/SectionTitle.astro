---
// src/components/SectionTitle.astro
import { cn } from "../lib/utils";

interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<h2 class={cn("text-4xl font-bold text-center mb-12", className)}>
  <slot />
</h2>